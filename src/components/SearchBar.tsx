function SearchBar() {
  return (
    <div>
      <form className="searchBar">
        <input type="text" />
        <button type="submit">Find</button>
      </form>
    </div>
  );
}

export default SearchBar;
